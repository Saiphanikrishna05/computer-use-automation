node_modules/
.env
.env.local
*.log

# Run output that is regenerated by the demo path. Curated bundles under
# evidence/ are committed deliberately (see evidence/README.md).
runs/
.playwright/
test-results/
.DS_Store
.docs-shot*.tmp.ts
*.bak
# Anchored. Unanchored these match at any depth, and `audit/` silently
# swallowed src/audit/ -- the repo built here and not from a clean clone.
/audit/
/audit-log.jsonl
# The recording is regenerated by `npm run record`; the script is the artefact.
